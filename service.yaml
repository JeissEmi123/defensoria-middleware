# NOTE: Template file — do not commit production secrets here.
apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: defensoria-middleware-prod
spec:
  template:
    metadata:
      annotations:
        run.googleapis.com/cloudsql-instances: sat-defensoriapueblo:us-central1:defensoria-db
        run.googleapis.com/startup-cpu-boost: 'true'
    spec:
      containerConcurrency: 80
      timeoutSeconds: 900
      containers:
      - image: gcr.io/sat-defensoriapueblo/defensoria-middleware:latest
        ports:
        - containerPort: 8080
        env:
        - name: APP_ENV
          value: production
        - name: EMAIL_SERVICE
          value: none
        - name: SECRET_KEY
          value: REPLACE_ME
        - name: JWT_SECRET_KEY
          value: REPLACE_ME
        - name: JWT_REFRESH_SECRET_KEY
          value: REPLACE_ME
        - name: ADMIN_DEFAULT_PASSWORD
          value: REPLACE_ME
        - name: POSTGRES_USER
          value: REPLACE_ME
        - name: POSTGRES_PASSWORD
          value: REPLACE_ME
        - name: POSTGRES_DB
          value: REPLACE_ME
        - name: POSTGRES_PORT
          value: "5432"
        - name: DATABASE_URL
          value: REPLACE_ME
        resources:
          limits:
            cpu: '2'
            memory: 4Gi
        startupProbe:
          tcpSocket:
            port: 8080
          initialDelaySeconds: 0
          timeoutSeconds: 5
          periodSeconds: 10
          failureThreshold: 24
